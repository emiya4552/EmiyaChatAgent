# 账户与访问原型

评审问题：日常偏好、高级生成默认、安全设备管理和认证路径如何在同一套克制的体验中成立？

运行：在 `emiya-frontend/` 下执行 `npm run prototype:account`。使用 `?variant=A|B|C` 直达方案。

覆盖：`/settings` 全部分区，以及 `/login`、`/register`、`/forgot-password`、`/reset-password`。

这是只读、无后端调用的可删除原型。
