# 对话工作区原型

评审问题：怎样让聊天、开场配置、对话级控制与 MVU 卡 UI 同时可用而不破坏沉浸感？

运行：在 `emiya-frontend/` 下执行 `npm run prototype:conversation`。使用 `?variant=A|B|C` 直达方案；底部切换器和左右方向键可切换。

覆盖：`/chat`、新建对话、对话设置、输出契约状态、开场白切换、情绪/关系提示、MVU Host Dock。

这是只读、无后端调用的可删除原型。选定方向后，将其拆入正式聊天组件并删除本目录。
