# 创作资产工作台原型

评审问题：角色、世界书、预设、模板和正则这些高密度创作资产，应怎样共享导航、列表和编辑心智模型？

运行：在 `emiya-frontend/` 下执行 `npm run prototype:studio`。使用 `?variant=A|B|C` 直达方案。

覆盖：角色管理/详情/表单/导入导出、世界书管理/编辑/输出契约、预设、Prompt 模板和正则预设的管理与编辑。

这是只读、无后端调用的可删除原型。选定方向后再按资源类型重写为生产组件。
