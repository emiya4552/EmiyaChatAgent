# 记忆与感知原型

评审问题：记忆的人工治理、情绪统计和关系变化怎样在“理解故事”而非“监控数据”的框架中协作？

运行：在 `emiya-frontend/` 下执行 `npm run prototype:insights`。使用 `?variant=A|B|C` 直达方案。

覆盖：`/memories` 的筛选/编辑/删除/清空，`/mood` 的范围筛选、趋势、分布、日历，以及聊天/角色详情中的关系和里程碑。

这是只读、无后端调用的可删除原型。
